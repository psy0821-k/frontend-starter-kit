# 좋아요/북마크 기능 — 초기 아이디어

## 한 줄 정의

템플릿(templates) 및 기능(features)에 좋아요/북마크 기능을 추가한다.

## 배경

사용자가 마음에 드는 템플릿이나 기능을 표시해두고 나중에 다시 찾아볼 수 있게 한다.

## 대상

- `templates/[id]` (및 목록) — DB 기반(Supabase `templates` 테이블)
- `features` — 현재 정적 하드코딩 데이터(`feature-catalog/model/data.ts`), DB 테이블 없음
