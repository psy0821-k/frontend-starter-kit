-- ============================================================
-- 006. 템플릿 라이브 프리뷰 URL 컬럼 추가 (v1, 이후 폐기)
-- ============================================================
-- 상세 페이지에서 배포된 실제 화면을 iframe으로 보여주기 위한 컬럼이었다.
-- 결정 배경(v1): docs/templates/feature/detail/spec-fixed.md 이력 참조.
--
-- 이 방식은 이후 Sandpack 기반 즉석 렌더링(v2, is_entry 필드)으로 완전히
-- 대체되어 007_drop_template_preview_url.sql에서 컬럼을 제거한다.
-- 로컬 마이그레이션 파일이 누락되어 있던 것을 원격 이력 기준으로 사후 복원한
-- 것이며, 현재는 유효하지 않은 컬럼이다(이력 보존 목적).
-- ============================================================

alter table public.templates
  add column if not exists preview_url text;
